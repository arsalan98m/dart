// Basic Types are int, double, String and bool
// Dart is case sensitive language this means writing variable name age in lowercase is not the same as writing variable name Age with a capital letter they are different.
void main() {
  String name = 'arsalan';
  int age = 36;
  double height = 1.84;
  bool likesDart = true;

  print(name);
  print(age);
  print(height);
  print(likesDart);
}
